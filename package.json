{
  "name": "awsome-chat-infra",
  "version": "0.1.0",
  "scripts": {
    "build": "cd app/backend && npm run bundle",
    "install": "cd app/backend && npm install && cd ../../terraform && terraform init"
  },
  "devDependencies": {},
  "dependencies": {}
}
